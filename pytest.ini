# pytest.ini
[pytest]
DJANGO_SETTINGS_MODULE = Django_star.settings
python_files = test_*.py *_test.py
testpaths =
    tests
    apps
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    --capture=no